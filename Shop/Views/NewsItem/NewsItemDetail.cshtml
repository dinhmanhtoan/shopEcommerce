@model NewsItemVm

@{
    ViewData["Title"] = Model.Name;
}
<div class="news-detail">
        <h3 class="news-title">@Model.Name</h3>
        <div class="thumbnail">
            <img src="@Model.ThumbnailImageUrl" alt="" />
        </div>
        <div class="news-item-full-description">
            @Html.Raw(Model.FullContent)
        </div>
</div>